# 정보 알고리즘 스튜디오 제품 요구사항 명세서

> 최종 갱신: 2026-09-13 · 기능 요구사항과 상태의 단일 기준(SSOT)
> 사용자 승인으로 GitHub·Vercel·Firestore 운영 반영을 완료했습니다. 교사 역할 연결 후 사용자가 Google 로그인 성공을 확인했습니다. 실제 DB 구성은 [데이터베이스 설계](docs/DATABASE.md), 운영 검증은 [배포 기록](docs/DEPLOYMENT.md)을 확인합니다.

## 1. 목적과 문서 기준

중학교 정보 수업의 **추수활동**에서 문제 추상화·알고리즘 설계·순서도를 쉽고 부담 없이 적용하도록 돕습니다. 일반 실습은 도움을 받아 완성하는 성공 경험을, 수행평가 탭은 제공된 점검 기능을 활용해 올바른 알고리즘을 완성하는 능력의 평가를 지향합니다.

교육 철학은 [INTENT.md](INTENT.md), 기술·협업 원칙은 [AGENTS.md](AGENTS.md), 운영 안내는 [README.md](README.md)를 따릅니다. 종전 세부 수치와 변경 기록은 [이전 PRD 보존본](docs/PRD_HISTORY_2026-09-12.md)에 보관합니다. 보존본의 상충하는 설계나 완료 표현은 현재 요구사항으로 적용하지 않습니다. 여기서 재확정하지 않은 세부 수치는 기존 코드 현황으로 취급하며, 이번 정비가 해당 기능의 삭제를 뜻하지 않습니다.

## 2. 학습 구성

개념 → 퀴즈 → 실습을 기본 동선으로 합니다. 단원별 2차시(45분씩)를 기본 수업 흐름으로 삼되 교사가 필요한 추수활동을 선택할 수 있도록 합니다.

| 코스 | 활동과 목적 |
|---|---|
| 문제 추상화 | 맛보기와 생각 다이어트 워크북으로 현재·목표 상태, 조건, 핵심 요소 구분 |
| 알고리즘 설계 | 샌드위치 로봇 명령 조립·실행으로 알고리즘 조건과 오류 수정 경험 |
| 순서도 연구소 | 순차(라면) → 선택(놀이기구) → 반복(비밀번호) → 예시(등교) → 나만의 백지 |
| 수행평가 | 별도의 도움·제출·채점 규칙 아래 답안과 순서도 작성 |
| 교사용 화면 | 평가 시작·종료, 제출 상태·답안 확인, 성적 검토·내보내기 |

## 3. 실습과 수행평가의 확정 정책

| 항목 | 일반 실습 | 수행평가 |
|---|---|---|
| 목표 | 도움을 받으며 완성하고 효능감 얻기 | 점검 기능을 활용해 올바른 알고리즘 완성 |
| 도움 | AI 조언과 단계적 힌트, 재실행 권장 | 문제 조건 후보만 AI로 추천 가능, 풀이 생성 금지; 연결·실행 사실 안내 |
| 논리 오류 | 학생이 수정하도록 보완 안내 가능 | 학생 입력으로 한 번 실행, 목표 충족 여부는 학생이 확인 |
| 정답 제공 | AI 순서도 자동 완성 없이 학생이 직접 조립·수정 | 정답·수정 방법·고쳐야 할 논리 블록 직접 지목 금지 |
| 제출 | 보완을 권하되 현재 상태로 제출 가능 | 미완성·오류·검사 미통과여도 제출 가능 |
| 평가 | 성공 경험과 다음 시도 지원 | Part 1·2 자동 채점 + 자유 설계 4항목 각 10점, AI 초벌 제안 후 교사 확정 |

### 3.1 일반 실습

검사 → 힌트 확인 → 수정 → 재실행을 권장합니다. 검사 미통과나 AI 장애를 제출 차단 사유로 삼지 않습니다. AI 장애에도 기본 조립·실행·결과물 내보내기가 가능해야 합니다. 네트워크가 필요한 외부 게시 자체의 오프라인 성공을 보장하지는 않습니다.

AI 의견, 구조 검사, 실행 결과를 구분합니다. AI 미응답을 합격으로 대체하거나 AI 의견을 공식 정답 인증으로 표현하지 않습니다. 순서도나 자연어 기획을 수정하면 이전 검사 결과를 무효화합니다. **AI 합격을 제출 잠금 해제 조건으로 삼는 이전 정책은 폐기합니다.**

### 3.2 수행평가

새 자유 설계 회차의 흐름은 **자기 문제의 현재·목표 상태 정리 → 필요한 조건 선택·작성 → 자연어 알고리즘 작성 → 순서도 조립 → 실행·수정 → 제출**입니다. 이전 고정 주제 회차는 당시 문항과 채점 기준을 보존합니다.

* Part 3은 학생이 자기 문제를 분석하는 빈 처방전을 제공합니다. 현재·목표 상태는 학생이 직접 쓰고, 조건도 직접 입력하거나 AI의 제약 조건 후보 중 필요한 것을 골라 수정합니다. AI가 행동 순서나 완성된 풀이를 작성하지 않습니다. 실습과 같은 순차·선택·반복 카드를 직접 추가·수정·재배열하며 세 구조를 모두 쓰도록 요구하지 않습니다. 일반 실습의 AI 처방전은 유지합니다.
* 처방전은 답안과 함께 저장·복원하고 교사 답안 화면에 표시합니다. 승인된 자유 설계 기준은 문제·조건의 명확성, 자연어 알고리즘의 논리성, 자연어와 순서도의 일치, 순서도 구조·동작의 타당성 각 10점입니다. 문장력·단계 수·AI 사용 여부를 채점 기준으로 추가하지 않으며 미작성도 제출을 막지 않습니다.
* 평가가 시작되면 다른 코스·교사 화면·전체 메뉴로의 앱 내 이동을 막고 Part 1~3 전환은 허용합니다. 같은 탭 새로고침 시 진행 중인 평가로 복귀합니다. 제출 저장에 실패하면 잠금을 유지하며 저장 성공 후 해제합니다. 대기 중에는 이동할 수 있습니다. 다른 브라우저 탭·외부 사이트까지 차단하는 기능은 아닙니다.
* 각 파트 하단에도 이전·다음 이동 버튼을 제공합니다. 미응답이 있어도 이동할 수 있고 작성 개수와 건너뛰기 안내를 표시합니다. 모든 문항에 응답했을 때도 자동으로 이동하지 않습니다.
* 수동 최종 제출은 Part 3에 한 번 방문한 뒤 허용합니다. 현재 파트와 방문 상태는 같은 탭 새로고침 후 복원하며 교사의 재시험 초기화 시 함께 지웁니다. 시간 만료·교사 종료에 따른 자동 제출은 방문 여부와 무관하게 진행합니다. 미완성 답안도 제출할 수 있고, 실제 저장 성공 응답 후에만 완료로 표시합니다.
* 새 평가의 객관식 보기에서 영어 병기를 제거하고 제목은 `N번 문제`로 표시하여 정답 단서를 없앱니다. 단답형 5·6번은 변수·디버깅 용어 대신 선택 조건에 따른 결과와 반복 횟수를 묻습니다. 문항 수·배점·시간은 유지합니다. 회차별 문항 버전을 보존하여 기존 답안을 새 기준으로 재채점하지 않습니다.

* 연결 누락과 실행이 막힌 위치를 하이라이트 등으로 표시합니다. 색상 외에도 짧은 안내를 제공합니다.
* “이 단계에서 실행을 이어갈 수 없어요”처럼 관찰된 사실을 안내합니다. 중단 위치와 근본 원인이 같다고 단정하지 않습니다.
* 학생용 실행은 실습과 같은 입력·판단 상호작용으로 한 번의 흐름을 진행합니다. 입출력 기호가 입력을 요구하면 값을 받으며, 자연어 판단은 학생이 예·아니요를 직접 선택할 수 있습니다. 반복 구조가 입력으로 돌아오면 다시 값을 받습니다. 종료 도착을 정답으로 단정하지 않습니다. 이전 회차의 지정 검사 입력은 기존 참고 점수 계산에만 남기고 학생 실행 버튼에서 여러 번 자동 재생하지 않습니다.
* 평가 중 일반 실습용 AI 풀이 조언이 유입되지 않아야 합니다. 실행 엔진이 확인한 사실을 기반으로 피드백합니다.
* 제출 후 교사용 화면에서 Solar AI 초벌 채점을 요청할 수 있습니다. 항목별 점수·근거·불확실성을 제안으로 저장하며 교사가 답안을 보고 점수를 확인·수정하여 확정합니다. AI 오류·해석 불가·미확정은 0점이 아닌 채점 대기로 표시합니다. 확정 전 총점과 CSV 최종 점수도 채점 대기로 남깁니다.
* **오류 수정 이유 한 문장, 추가 성찰 보고서, 힌트 사용 횟수·수정 과정에 따른 별도 채점이나 감점을 추가하지 않습니다.** 기존 단답형 문항을 삭제한다는 뜻은 아닙니다.
* 풀이 완성 여부와 제출 가능 여부를 구분합니다. 제출 마감·권한·저장 상태는 별도 운영 규칙을 따릅니다.

## 4. 기술·화면 방향

프런트엔드는 HTML/CSS/Vanilla JS를 유지합니다. 현재 Tailwind CDN·로컬 CSS·외부 SDK를 사용합니다. 기본 환경은 **Vercel 웹 배포 + Firebase 연동 + 서버를 통한 AI 호출**입니다. 학생에게 개발 도구 설치나 빌드를 요구하지 않습니다. 파일 더블 클릭이나 Netlify 폴더 업로드만으로 서버 기능까지 동작할 것을 요구하지 않습니다.

Node.js 서버리스 API와 개발용 자동 검증 도구를 허용합니다. 프레임워크 전환은 현재 범위에 포함하지 않습니다. 향후 구조 개선은 순서도 데이터·실행·검사, 화면 조작, 외부 통신, 모드별 정책을 분리하고 실습·평가의 공통 엔진 사용을 지향합니다. 구체적인 코딩은 구현 계획을 설명한 뒤 진행합니다.

순서도는 단말(보라), 자료(초록), 판단(주황), 처리(파랑)와 직각 연결선을 사용합니다. 학생이 블록과 연결을 직접 구성합니다. 백지 공방의 자연어 기획·기호 보관함·캔버스·실행 정보 영역을 유지하되 고정 열 수나 픽셀 정렬보다 주요 조작과 작업 영역의 가시성을 우선합니다. 최종 지원 화면 범위는 교실 환경 확인 후 확정합니다.

개념·퀴즈·학습 지도는 최대 1,200px로 중앙 정렬하여 넓은 화면에서 양쪽 여백을 둡니다. 실습·순서도 작성은 가용 폭을 사용합니다. 수행평가 Part 1·2는 읽기 폭을 제한합니다. Part 3은 `나만의 백지` 편집기를 공유하여 자연어 기획서·세로 기호 보관함·캔버스·접을 수 있는 실행 패널을 제공합니다. 좁은 화면에서는 세로로 배치하며 문제 안내도 접을 수 있습니다. 실행·한 단계 조작과 최근 안내는 실행 패널이 접혀 있어도 접근할 수 있습니다.

### 4.1 상단 탐색과 공통 UI 정비 — 구현, 검증 기록 별도

2026-09-12 사용자가 문서 지침의 구현과 배포를 승인했습니다. 변경 범위와 검증 근거는 [UI 개편 검토서](docs/UI_REVIEW.md)를 확인합니다.

* 교사용 클래스룸 진입점을 학생 코스 메뉴에서 분리하여 우측 상단 관리 영역으로 옮깁니다. 아이콘과 짧은 `교사용` 이름을 함께 제공하고, 홈 화면의 중복 진입점은 정리하여 일관된 경로를 유지합니다.
* 전체 메뉴는 학습 단계별 바로가기 기능이 있어 유지하며 교사용 버튼 옆에 배치합니다. 호버 자동 열기·닫기를 제거하고 버튼 클릭, Enter, Escape, 외부 클릭과 초점 이동으로 제어합니다. 펼침 상태와 현재 메뉴를 접근성 속성으로 표시합니다.
* 상단은 최대 1,600px의 공통 폭을 사용합니다. 창 폭 1,350px 이하에서는 학습 메뉴를 둘째 줄에 배치합니다. 중복 홈 메뉴는 제거하고 이름이 있는 로고 버튼으로 돌아갑니다. 430px 이하에서는 네 학습 메뉴를 두 열로 배치합니다. 교사용·전체 메뉴의 이름은 작은 화면에서도 유지합니다.
* 실제 헤더 높이를 관찰하여 실습 캔버스의 남은 높이를 계산합니다. 순서도 단계 메뉴도 줄바꿈을 허용하고 작업 영역이 버튼을 덮지 않게 합니다. 평가 중 이동 잠금·남은 시간·제출 동작은 유지합니다.
* 홈의 큰 홍보 배너와 중복 소개를 줄이고 세 학습의 개념·퀴즈·실습 진입점을 제공합니다. 기존에 연결 대상이 없던 심화 탐구 진입점은 제거합니다. 수행평가는 별도 안내 행에서 입장합니다.
* 공통 시각 기준은 본문 16px, 읽기 최대 폭 1,200px, 기본 모서리 12px, 중립색 배경과 제한된 파란 강조색입니다. `css/studio-ui.css`의 CSS 변수로 관리하며 외부 폰트 다운로드 대신 시스템 글꼴을 기본 대안으로 사용합니다. 개념 설명 배너의 그라데이션·그림자를 줄이고 교육용 기호와 색은 보존합니다.
* 캔버스 조작 안내는 기호를 덮지 않게 작업 영역 밖에 표시합니다. AI 도움 버튼은 전체 이름이 보이도록 배치하고 감소된 움직임 설정을 지원합니다. 교사·평가 도구 모음은 좁은 창에서 줄바꿈합니다.

### 4.2 순서도 작업 공간 보완 — 운영 배포 완료

2026-09-12 사용자 승인에 따라 `codex/flowchart-workspace-polish`에서 구현했습니다. 기준 버전은 `df293d8`이며 검증 범위와 화면은 [작업 공간 검토서](docs/WORKSPACE_UI_REVIEW.md)에 기록합니다. 2026-09-13 사용자 승인으로 아래 4.3·4.4와 함께 main에 병합하고 운영 배포했습니다. [배포 기록](docs/DEPLOYMENT.md)을 확인합니다.

* 세 학습 코스의 작은 아이콘과 선택된 탐색 버튼에 제한적인 색상 구분을 적용합니다. 수행평가는 간격과 테두리로 분리하며, 로고의 홈 이동 및 평가 중 이동 잠금을 유지합니다.
* 결과 화면은 큰 점수와 작은 `교사 검토 전` 표시를 분리합니다. `자동 채점 참고 점수`로 안내하며 배점과 채점 로직은 변경하지 않습니다. 휴대전화 폭에서는 세 부분 점수를 세로 행으로 표시합니다.
* 순서도 다섯 단계의 공통 상단 여백은 20px, 탐색 영역 최소 높이는 58px, 본문 간격은 12px입니다. 같은 창 폭에서는 단계 전환 전후 기준선이 유지되며, 좁은 창에서는 단계 버튼이 줄바꿈합니다.
* 세로 기호 보관함과 직접 드래그·더블클릭 추가 경험을 유지합니다. 백지 공방의 기획서 폭은 300~320px, 기호 보관함은 100px입니다. 짧은 제목·버튼은 끊지 않고 자연어 본문은 단어 단위 줄바꿈을 우선합니다.
* 실행 패널은 최초에 접어 두고 학생이 직접 펼칩니다. 펼친 폭은 240px, 접힌 띠는 44px이며 `실행 과정`을 바로 선 세로 글씨로 표시합니다. 띠 전체와 키보드로 펼칠 수 있습니다. 900px 이하의 기존 세로 배치에서는 가로 접기 버튼으로 전환합니다.
* 실행·정지·한 단계 조작과 최근 실행 메시지는 패널을 접어도 보입니다. 실행 시 패널을 자동으로 펼치지 않고 작성한 블록 좌표도 변경하지 않습니다. 실행 기록 초기화 시 요약 메시지도 초기화합니다.
* 기본 드래그 스냅샷 대신 Canvas API로 네 기호의 모양을 그려 미리보기에 사용합니다. 실제 배치 중심은 도형 크기·화면 이동·확대 배율을 반영합니다. 새 기호를 휴지통에 놓는 동작이 기존 선택 블록을 삭제하지 않도록 구분합니다.

### 4.3 수행평가 문항·작성·이동 보완 — 운영 배포 완료

2026-09-13 사용자 승인에 따라 `codex/assessment-guided-workspace`에서 구현했습니다. 기준 버전은 앞선 작업 공간 보완 커밋 `432a4fd`이며 후속 자유 설계 개선과 함께 운영 배포했습니다. 검토 절차와 당시 검증 근거는 [수행평가 검토서](docs/ASSESSMENT_UI_REVIEW.md)에 기록합니다. 새 회차의 문항과 실행 방식은 아래 4.4가 우선합니다.

* 실습의 편집기 DOM과 기호 드래그·연결·블록 편집·화면 이동·정렬을 공유합니다. 평가 진입 전 실습 상태를 별도로 보존하고 평가 이탈 시 복원하여 두 답안이 섞이지 않게 합니다. 기존 평가 전용 편집 코드는 제거했습니다.
* 평가에서는 AI 검사·처방전 자동 생성·예시 불러오기·외부 공유를 숨기고 호출도 차단합니다. 실행 패널에는 평가 실행기의 입력·진행 단계·중단 사실·예상/실제 결과를 표시합니다. 해석할 수 없는 자연어는 교사 검토 대상으로 남기며 수정 풀이를 생성하지 않습니다.
* 순차·선택·반복 카드의 필드와 순서를 저장·복원하고 교사 화면에도 구조별로 표시합니다. 이전의 짧은 텍스트 단계도 읽을 수 있습니다. 설계나 순서도 편집 후 이전 실행과 하이라이트를 초기화합니다.
* 이 단계에서 구현한 문항 버전 2는 이전 회차 호환용으로 보존합니다. 현재 새 회차는 아래 4.4의 버전 3을 사용합니다. 버전 없는 기존 회차는 버전 1로 유지하며 기존 회차의 시작·재시험이 문항을 바꾸지 않습니다. 교사 참고 점수는 학생 답안이 주장하는 버전이 아닌 교사 관리 회차의 버전으로 계산합니다.
* 기본 검사 14개, 기존 브라우저 시나리오 23개, 작업 공간 UI 5개, 수행평가 UI 9개를 로컬 모의 환경에서 검증합니다. 통과 기록은 검토서와 테스트 결과 파일을 기준으로 하며 운영 Firebase·Google 인증·학급 동시 접속을 재검증한 것으로 간주하지 않습니다.

### 4.4 자유 설계·단일 실행·AI 초벌 검토 — 운영 배포 완료

2026-09-13 `codex/assessment-open-design`에서 `24bdc46`을 기준으로 구현했습니다. 위 4.3의 고정 주제 방식은 이전 회차용으로 보존하며 새 회차는 문항 버전 3을 사용합니다. 구체적인 한계와 검증 기록은 [자유 설계 검토서](docs/ASSESSMENT_OPEN_DESIGN_REVIEW.md)에 기록합니다.

사용자의 병합·배포 승인으로 제품 `0d777f4`와 DB 조회 도구 `c456626`을 main `ade1f7b`에 병합했습니다. 운영 파일 일치, 세 창 폭의 기본 동작, API 메서드·인증 차단을 확인했습니다. 실제 Solar 채점 품질과 교사·학생 공동 운영은 이번 배포 검사 범위에 포함되지 않습니다. 기존 평가 자료를 바꾸지 않았으므로 새 기능을 시험하려면 기존 평가 종료 후 새 평가를 준비합니다.

* 객관식의 같은 행에 놓인 두 문항은 질문 길이가 달라도 보기 버튼의 시작 높이를 맞춥니다. 9번 정답의 `입출력` 괄호 설명을 포함해 보기의 괄호 부연을 제거합니다.
* 네 고정 주제 대신 현재·목표 상태와 조건을 입력하는 상단 처방전을 제공합니다. 조건 추천 API는 일반 실습의 풀이 생성 API와 구분하고 학생이 선택·수정하도록 합니다. 로컬 시연은 실제 AI 호출 없이 명시적인 예시만 제공합니다.
* 기존 공통 실행기로 입력값 한 번의 흐름을 확인합니다. 입력 대기 다음에 분기 선택 대기가 연속해서 나오는 경우에도 연속 실행 의도를 유지합니다. 구조 오류·중단 위치는 공통 실행 패널에 표시합니다.
* 교사용 AI 검토 API는 인증된 교사 역할을 서버에서 확인하고 현재 회차의 제출 답안을 Firestore에서 직접 읽습니다. 학급·번호를 모델에게 전달하지 않고 현재·목표·조건·카드·기호·연결만 보냅니다. 답안 본문에 학생이 직접 쓴 개인정보까지 자동 제거하는 기능은 아닙니다.
* AI 출력은 네 항목의 정수 0~10점과 근거 형식을 검사하고 총점은 서버에서 계산합니다. 답안 내 지시는 데이터로 취급하도록 고정 프롬프트를 사용합니다. 이 조치가 모델의 판단 정확성이나 지시문 공격 완전 차단을 보장하지는 않습니다.
* AI 제안과 교사 확정을 분리 저장하고 당시 회차·답안과 묶습니다. 확정 저장은 교사의 명시적 검토 확인 후에만 가능하며, 답안·회차가 달라지면 오래된 결과를 거부합니다. 실제 Solar 품질과 운영 인증·비용·동시 접속은 이번 모의 검사로 검증한 것이 아닙니다.

### 4.5 교사 평가 상태와 단일 조작 버튼 — 운영 배포 완료

2026-09-13 사용자가 구현·간단한 검증 후 main 병합과 운영 배포까지 승인했습니다. `codex/teacher-session-control`에서 구현했습니다. [교사 제어 검토서](docs/TEACHER_CONTROLS_REVIEW.md), [현재 PC·AI 인수인계](docs/HACKATHON_HANDOFF.md)를 확인합니다.

제품 `363a49a`를 main `dfdf7e0`에 병합·푸시했습니다. Vercel Production 성공과 기존 도메인의 파일 15개 일치·주요 화면 동작을 확인했습니다. [운영 배포 근거](docs/DEPLOYMENT.md)를 확인합니다.

* 학급 상태와 조작을 분리합니다. 준비 전 → 입장 대기 → 평가 중 → 평가 종료에 따라 같은 위치의 버튼이 평가 준비 → 평가 시작 → 평가 종료 → 새 평가 준비로 전환됩니다. 성적표 CSV는 보조 버튼으로 유지합니다.
* 입장·제출 인원과 평가 중 남은 시간을 상태 영역에 표시합니다. 입장 기록 수를 실시간 접속자 수라고 부르지 않습니다. 평가 종료는 채점 완료와 구분합니다.
* 종료는 확인창을 거치고 윤곽선 버튼으로 표시합니다. 새 회차 준비에도 이전 답안을 보관하고 다시 입장해야 한다는 확인을 제공합니다. 시작은 추가 확인창 없이 수행하며 처리 중에는 재클릭·학급 변경을 잠급니다.
* 서버 처리 성공 전에는 완료 상태를 표시하지 않습니다. 실패는 상태 영역에서 안내하고 재시도할 수 있습니다. 상태 구독 실패 시 시작·종료 대신 다시 연결을 제공합니다. 학급 전환·교사 화면 이탈 때 이전 구독과 타이머를 해제합니다.
* 다른 교사 화면의 회차 변경을 구독하며, 시작·종료·준비 요청은 당시 회차·상태와 서버 기록을 트랜잭션에서 비교합니다. 오래된 화면의 종료 요청이 새 회차를 마감하지 않게 합니다.
* 제한 시간이 지나면 화면에 평가 종료로 표시합니다. 이 표시만으로 DB를 쓰지 않으며 새 평가 준비를 선택했을 때 종료되지 않은 이전 회차를 마감한 뒤 보관·준비합니다. 기존 학생 자동 제출 정책과 배점·시험 시간은 유지합니다.
* 기본/API 검사 21개·교사 UI 검사 8개·학생 평가 회귀 10개를 통과했습니다. 운영 계정으로 실제 시험을 시작·종료한 검사는 아니며 배포 후 읽기 전용 확인을 별도로 기록합니다.

## 5. 저장·운영·접근 제어 요구사항

다음은 목표 요구사항입니다. 현재 상태는 6절을 확인합니다.

* 브라우저 임시 작업은 sessionStorage를 사용하고 학생 정보·작업을 localStorage에 저장하지 않습니다. Firebase 서버 저장은 학생·교사 권한을 구분합니다.
* 서버의 저장 성공 응답 전에는 제출 완료를 표시하거나 답안을 잠그지 않습니다. 실패 시 작성 내용을 유지하고 재시도 경로를 제공합니다.
* 새로고침·동일 학생 재접속이 기존 답안을 초기화하지 않아야 합니다. 다음 차시 이어하기 방식과 서버 보관·삭제 기간은 아직 미정입니다.
* 교사의 평가 종료·재시험 허용이 학생 상태에 반영되어야 합니다. 마감 기준, 중단·재접속 시 시간 처리, 재시험의 답안 유지·초기화 정책은 구현 전에 확정합니다.
* 교사 권한, 학생별 답안 접근, 제출 후 수정 제한은 서버에서 검증합니다. 클라이언트 PIN만으로 권한을 보장하지 않습니다.
* AI 비밀 키는 서버에서 관리합니다. 브라우저 키 직접 호출 경로는 운영 배포 기준에 맞게 정비해야 합니다. 프록시 접근 제어와 호출 제한도 필요합니다.
* 학생 입력·AI 출력은 안전하게 표시하고 연결·저장 상태는 실제 결과를 반영합니다.
* 자동 채점에는 문항별 기준과 근거가 있어야 합니다. 기호·연결선 개수만으로 순서도 논리 정답을 보장하지 않습니다. 교사는 기존 기준으로 최종 결과를 검토합니다.

## 6. 현재 구현과 검증 상태

상태 용어: **계획**(합의됐으나 미반영), **구현됨·검증 필요**(코드 존재), **오류 확인**(재현 또는 코드 근거 있음), **검증 완료**(특정 버전·환경·사례의 통과 근거 있음). 일부 정상 동작을 전체 기능의 검증 완료로 확대하지 않습니다.

2026-09-12 원본 감사의 기준은 Git `cacfc5e`이며 [감사 보고서](audit/AUDIT_REPORT.md)와 [발견 사항](audit/findings.json)을 보존합니다. 이후 변경은 [구현 검토서](docs/IMPLEMENTATION_REVIEW.md)와 [배포 기록](docs/DEPLOYMENT.md)에 별도로 기록합니다. 원본 35건을 모두 해결했다고 간주하지 않습니다. 실제 학생 권한 검사 13개와 AI 응답 1회는 확인했습니다. 교사 Google 로그인은 사용자가 성공을 확인했으며, 학급 동시 접속은 아직 미검증입니다.

| 영역 | 현재 상태 | 주요 차이·근거 |
|---|---|---|
| 개념·퀴즈·실습·화면 전환 | 수정 / 로컬 검증 | 화면 중첩 제거, 캔버스 높이·툴바, 개념 문구·색상 보완 |
| 순서도 실행·검사 | 수정 / 지정 사례 검증 | 한글 변수 보존, 누락 분기 중단, 잘못된 수식 거부 |
| AI 및 실습 제출 | 수정 / 모의 장애 검증 | 제출 게이트 제거, 장애를 장애로 표시, 수정 후 검사 무효화 |
| 수행평가 피드백 정책 | 구현 / 제한된 표현·입력 검증 | 구조 문제·중단 위치 표시, 예상/실제 결과 비교, 정답 수정 방법 미제공 |
| 수행평가 저장·복구·채점 | 수정 / 로컬·모의 검증 | 저장 확인 후 완료, 초안 복원, 제출 재시도, 답안 기반 재계산 |
| 교사 권한·실시간 운영 | 학생 원격 권한 검사 통과 / 사용자 교사 로그인 확인 | Google·익명 공급자 활성화, 학생 권한 검사 13개; 교사 UID 역할 연결 완료, 학급 공동 운영 별도 검증 필요 |
| Vercel·Firebase 운영 | 운영 배포 완료 / 제한된 운영 검증 | Ready, 실제 익명 로그인·학급 조회·AI 응답 확인, 27명 동시 사용 미검증 |

현 코드의 평가 구성은 30분, 객관식 10문항 30점, 단답형 6문항 30점, 알고리즘 설계·순서도 40점입니다. 11개 반·27명은 현재 설정값이며 전국 공통 기준이 아닙니다. 온실·자판기·청소년 요금·횡단보도 테마의 동등 난이도와 채점 타당성은 미검증입니다. Part 3의 제시 방식과 작성 흐름을 변경했으며 문항 수·배점·시간은 유지합니다. 자연어 설계에 필요한 시간을 고려한 평가 시간 조정은 교사 확인 후 결정합니다.

## 7. 구현 후 확인할 완료 조건

아래는 수용 기준이며 통과 기록이 아닙니다. 수정한 기능마다 날짜·Git 버전·환경·사례·결과를 기록합니다.

1. 실습에서 검사 미통과·AI 실패에도 결과물 내보내기가 가능하고 보완 경로가 제공된다.
2. 평가에서 연결 누락·실행 중단·결과 불일치를 구분하고 정답·수정 방법을 노출하지 않는다. 미완성 답안도 제출할 수 있다.
3. 저장 실패 시 완료 표시 없이 답안이 유지된다. 새로고침·재접속 복구가 동작한다.
4. 무권한 교사 조작·다른 학생 답안 접근·마감 후 변경이 서버에서 거부된다.
5. 빈 그래프·끊긴 분기·잘못된 조건·반복 종료·입출력 오류를 포함해 실행 및 채점 결과를 확인한다.
6. 편집 후 검사 결과가 무효화되고, 초기화·화면 이동 뒤 이전 실행 타이머가 화면을 바꾸지 않는다.
7. 교실 대표 화면에서 주요 버튼·기호·캔버스를 사용할 수 있고 전환 후 이전 화면이 겹치지 않는다.
8. 평가에 일반 실습용 AI 풀이 도움, 추가 서술 요구, 힌트 감점이 유입되지 않는다.

## 8. 다음 구현 전 남은 결정

* 다음 차시 이어하기 방식, 서버 보관·삭제 기간
* 평가 마감·재접속·재시험 운영 세부 규칙
* 문항별 채점 기준, 검사 입력·예상 결과와 학생 공개 범위
* 실제 교실의 브라우저·화면 크기·동시 사용 인원 및 운영 검증 환경

이 항목들은 아직 확정되지 않았습니다. 우선순위는 감사에서 확인된 권한·저장·실행·채점 오류와 합의된 모드 정책 반영이며 기능 확장은 별도 협의합니다.

### 이번 구현의 운영 전제

교사 Google 로그인·학생 학급/번호/이름 + 내부 익명 인증은 승인된 방식입니다. 교사 역할은 `teachers/{uid}.enabled = true`로 관리자가 등록하며 학생 브라우저는 역할을 만들 수 없습니다. 브라우저 인증은 세션 단위입니다. 익명 인증은 실명·학번 증명이 아니므로 번호 오입력·선점·다른 PC 복귀의 교사 복구 절차는 운영 검증에 포함합니다.

평가 시간은 저장된 마감 시각과 현재 시각의 차이로 표시합니다. 서버 규칙은 마감 이후 답안 변경을 거부하고, 이미 서버에 저장된 동일 답안을 제출 상태로 확정하는 것은 허용합니다. 미전송 변경이 있는 마감·오프라인 복구는 교사 확인이 필요하며 자동 성공 처리하지 않습니다. 재시험 전 답안을 보관하며, 마감 후 새 평가 준비는 이전 학급 기록 보관·번호 문서 초기화·새 회차 생성을 트랜잭션으로 처리합니다. 보관 기간과 기간 연장 정책은 별도 결정 대상입니다.

이전 문항 버전 1·2의 참고 점수는 객관식 30 + 단답형 30 + 지정된 순서도 검사 입력 통과 40이며 기존 교사 총점 조정을 유지합니다. 문항 버전 3은 객관식 30 + 단답형 30만 자동 계산하고 자유 설계 40점은 교사가 항목별로 확정합니다. Solar는 초벌 점수와 근거를 제안하며 확정 성적을 결정하지 않습니다. 자유 주제 간 난이도와 AI 초벌 점수의 타당성이 검증된 채점기로 간주하지 않습니다.




## 9. 2026-09-13 해커톤 플랫폼 확장 — 1차 화면 구현, 기록·성적 연결 계획

사용자는 정보 수업용 다단원 학습·클래스룸·학습 기록·수행평가 플랫폼으로의 확장을 요청했다. 학생용 서비스 이름은 `정보 놀이터`로 변경한다(9.5). 앞선 문서의 `교수평기 올인원`은 이전 기획명이다. 최초 논의 당시 작업 시간은 약 4~5시간으로 설명했으며 현재 남은 시간은 별도로 확인하지 않았다. 개발 폴더는 `D:\hack_thon`이며 앞으로 이 위치를 유지한다.

현재 진행 상태는 [해커톤 인수인계](docs/HACKATHON_HANDOFF.md)를 따른다. 아래 절들은 확장 과정의 순서별 기록이다. 초기 화면은 9.4, 실제 로그인·활동 기록은 9.7~9.9, 픽셀 캠퍼스는 9.12, 실제 게스트 로그인은 9.13을 기준으로 읽는다. 자동 과정점수 환산은 아직 기획 범위다.

### 9.1 해커톤 전용 서비스 분리 — 서버 환경 변수 반영·관리 도구 대상 수정

사용자는 기존 서비스에 영향을 주지 않도록 새 GitHub 저장소·Vercel 프로젝트와 도메인을 사용하기로 했다. 2026-09-13 Antigravity의 `484b326`, 후속 Firebase 연결 `f43a5a2`를 확인했다. 상세 기록은 [환경 점검서](docs/HACKATHON_ENVIRONMENT.md)를 따른다.

* Git 원격은 `DonghyunT/donghyun-hackthon`, 현재 작업 브랜치는 `feature/hackathon`이다. 사용자가 제공한 Vercel 화면에서 이 브랜치가 Production 배포 대상으로 표시되므로 해당 브랜치 push도 배포 행동으로 취급한다.
* 새 Vercel 프로젝트는 `donghyun-hackthon`, 주소는 `https://donghyun-hackthon.vercel.app/`이다. CLI에서 Production Ready를 확인하고 Edge 1440×900에서 홈 표시·HTTP 200·페이지 JS 오류 0을 확인했다. 쓰기 요청을 차단한 읽기 전용 확인이다.
* 새 Firebase `donghyun-hackthon` 프로젝트와 Firestore `(default)` DB가 존재하고 메타데이터 조회에 성공했다. 후속 배포에서 브라우저 설정도 이 프로젝트를 가리키는 것을 확인했다. 새 인증 공급자·허용 도메인·보안 규칙·교사 역할·실제 저장은 이번에 검증하지 않았다.
* 최초 점검에서 없었던 `FIREBASE_PROJECT_ID`를 사용자 수정 요청에 따라 새 Vercel 프로젝트의 Production·Preview·Development에 `donghyun-hackthon`으로 등록하고 값·적용 환경을 재조회했다. API는 이 환경 변수를 기존 기본값보다 우선 사용한다. 기존 소스 기반 재배포 `dpl_B8p1y5DcK2FcMEmmjb5i8YNMXFv3`의 Production Ready와 새 도메인 연결을 확인했다.
* `tools/firebase-admin.cjs`의 대상을 `donghyun-hackthon`으로 수정했다. `.firebaserc`·생성되는 Firestore URL·문서 이름 일치와 수정 도구를 통한 새 DB 메타데이터 조회를 확인했다. 관리 도구 변경은 로컬 수정이며 커밋·푸시는 하지 않았다. 기존 DB는 변경하지 않았다.
* 검증은 문법 검사, 대상 설정·메타데이터 조회, 운영 API의 무인증 요청 차단에 한정한다. 실제 학생 로그인·답안 저장·유료 AI 호출의 정상 동작을 검증한 것은 아니다. Preview·Development 설정은 향후 해당 환경 실행·배포에 사용되며 이번에 새로 만든 배포는 Production이다.

### 9.2 과정중심평가를 위한 차시별 기록 — 교육 방향 합의, 세부 설계·구현 전

2026-09-13 사용자는 과정중심평가를 지향하며, 교사가 학생의 맥락을 고려해 판단할 수 있도록 가능한 한 정량적인 근거를 남기고자 한다고 설명했다. 이번 단계는 기획·PRD 정리이며 앱 구현이나 새로운 배점·수집 범위의 확정이 아니다.

**사용자가 밝힌 방향**

* 일반 수업의 한 차시 안에서 퀴즈 결과와 실습 결과를 학생별 데이터로 남긴다. 여러 차시의 기록을 연결해 학습 과정을 살펴볼 수 있도록 한다.
* 수행평가에서는 정해진 시간 동안 학생이 해결해낸 답안·결과물을 남긴다. 일반 수업 기록과 평가 당시 결과를 구분한다.
* 순서도 수행평가는 객관식·주관식 문항으로 정량적 근거를 확보하고, 순서도 제작 결과와 설계 내용을 통해 학생의 사고를 살피되 교사의 채점 부담이 커지지 않도록 한다.
* 정량 자료는 교사의 맥락 있는 판단을 돕는 근거이다. 기록의 목적과 성적 산출 규칙을 별도로 명세한다. 이번 설명만으로 새로운 과정 점수·합산식·배점·평가 시간을 도입하지 않는다.

**기록·화면 설계 제안 — 아직 미확정**

플랫폼 구조는 클래스 → 단원 → 차시 → 활동을 기준으로 검토한다. 교사는 학급의 차시별 퀴즈·실습 현황을 표로 살펴보고, 학생을 선택해 여러 차시의 결과와 원본 답안·결과물을 확인한다. 수행평가 결과는 평가 회차·당시 문항·기준에 연결해 별도로 표시한다.

| 상황 | 기록 후보 | 해석·표시 원칙 |
|---|---|---|
| 차시 퀴즈 | 문항별 답안·정오·정답 수/문항 수, 재풀이를 허용할 경우 첫 결과와 마지막 결과 | 같은 문항을 다시 풀어 얻은 결과와 첫 결과를 구분한다. 점수 차이만으로 성장 정도를 자동 확정하지 않는다. 재풀이 보관 범위는 별도 결정 |
| 차시 실습 | 학생 결과물, 제출 상태, 해당 활동에서 실제 확인 가능한 검사 결과 | 순서도 구조 검사·특정 입력 실행 결과 등 검사의 종류와 범위를 함께 표시한다. 종료 도착이나 기호 개수를 정답·성취도와 동일시하지 않는다 |
| 시간 제한 수행평가 | 당시 문항별 답안·항목별 점수, 계획·순서도 등 결과물, 회차·마감·제출 상태 | 평가 도움 범위와 제출 정책을 유지하고 AI 제안·교사 확정·미채점 상태를 구분한다. 기존 30/30/40점·30분 구성은 별도 합의 전 유지 |

* 여러 차시의 결과를 연결해 과정을 보는 것과 한 활동 안의 모든 수정 이력을 재생하는 것은 별도 기능이다. 중간 결과 보관이 필요한 경우 비교할 시점·종류를 먼저 결정한다. 클릭·키 입력·수정 횟수 등의 전수 기록은 확정 범위에 포함되지 않는다.
* 체류 시간·반복 횟수·AI 도움 횟수를 성실성·노력·능력 점수로 자동 환산하지 않는다. 관찰한 사실과 교사의 해석을 구분한다.
* 정량값은 원본 답안·결과물 및 당시 채점·검사 기준으로 확인할 수 있어야 한다. 미제출·저장 실패·미채점은 0점과 구분한다. 서로 다른 활동의 점수를 임의로 합산해 공통 성취 점수로 만들지 않는다.
* 기록을 만들기 위해 학생에게 별도의 과정 설명문이나 성찰 보고서를 추가하지 않는다. 교사는 요약에서 필요한 답안만 펼쳐 확인할 수 있게 한다.

**다음에 확정할 사항**

1. 일반 수업 퀴즈·실습 기록의 성적 반영 방식. 사용자가 제안한 수행평가별 과정 5점 안은 아래 9.3을 따르며, 교사 참고 수치와 공식 채점 수치를 구분한다.
2. 퀴즈 재풀이와 실습 중간 결과 보관 여부·범위. 저장 시점과 보관 기간, 학생에게 공개할 범위는 구현 전 확정한다.
3. 해커톤에서 먼저 구현할 대표 차시의 퀴즈·실습, 활동별 정량 지표와 교사 기록표의 최소 구성.

검증 상태: 문서 계획만 추가했다. 차시별 기록 저장·조회, 학생 식별과 차시 간 연결, 실제 채점 타당성·교사 업무량 감소는 아직 검증하지 않았다.

### 9.3 수행평가 3회와 제출 기반 과정점수 — 기획 방향 채택, 세부 기준 미정

2026-09-13 사용자는 현재 평가계획이 수행평가 3회, 각각 30점·30점·40점이라고 설명했다. 각 평가에서 제출 기반 과정점수 5점씩을 배정하는 논의 뒤, 이 내용을 바탕으로 빌드를 시작하자고 요청했다. 아래 구성을 플랫폼의 기획 기준으로 채택한다. 활동 인정·누락 처리·점수 환산 기준은 아직 미정이며, 이번 1차 화면 구조 작업에서 기존 채점 코드를 변경하지 않는다.

| 평가 | 총 배점(사용자 설명) | 과정점수(기획 기준) | 시간 제한 수행평가(기획 기준) |
|---|---:|---:|---:|
| 수행평가 1 | 30 | 5 | 25 |
| 수행평가 2 | 30 | 5 | 25 |
| 수행평가 3 | 40 | 5 | 35 |
| 합계 | 100 | 15 | 85 |

* 위 30·30·40은 **서로 다른 수행평가 3회의 배점**이다. 기존 순서도 평가 내부의 객관식 30·단답형 30·자유 설계 40점과 다른 구분이다. 어떤 회차에 순서도 평가를 배치할지, 기존 내부 점수를 25점 또는 35점으로 환산할지와 반올림 방식은 미정이다.
* 사용자 선호는 차시 활동 제출을 통해 과정점수 만점을 얻을 수 있게 하는 것이다. 퀴즈 정답률·실습 완성도 등 상세 자료는 교사의 학습 파악을 위해 남기되, 이를 과정점수의 추가 감점 요소로 자동 연결하지 않는다.
* 설계 제안: 각 수행평가에 관련 차시의 퀴즈·실습 활동 묶음을 연결하고 제출 현황을 보여준다. 과정점수와 수행평가 결과 점수를 구분해 표시한다. 실제 수행 내용과 원본 결과물을 확인할 수 있게 하며 미완성·오류 답안의 제출 경로는 유지한다.
* 추가 협의 제안: 수업 과제에 응답한 결과물이 제출됐는지 확인하는 최소 인정 기준, 내용이 전혀 없는 제출물의 보완 방법, 일부 활동 누락·지각 제출·결석·저장 장애의 처리 기준을 정한다. 공란·미제출·서버 저장 실패를 동일하게 취급하거나 일부 누락을 자동으로 과정 0점 처리하지 않는다. 이 제안은 확정 정책이 아니다.
* 낮은 퀴즈 점수나 오류가 있는 실습을 성적의 반복 감점으로 연결하지 않으면서, 기록과 피드백으로 다음 학습을 지원하는 방향을 검토한다. 제출 사실만으로 사고의 깊이·성장·노력을 측정했다고 표시하지 않는다.
* 학교의 실제 평가계획에 적용할 세부 평가 요소·배점·처리 방법은 학교의 해당 기준에 맞게 확정한다. 이번 문서는 특정 학교의 규정 적합성을 확인한 결과가 아니다. 참고: [교육부 학교생활기록부 종합지원포털의 평가계획 안내](https://star.moe.go.kr/web/contents/m30103.do?id=44718&schM=view), 2026-09-13 확인.

검증 상태: 5+25, 5+25, 5+35의 합계가 100점이며 과정점수 합계가 15점임을 확인했다. 제품 코드·기존 점수·평가 시간은 변경하지 않았다.

### 9.4 1차 빌드: 정보 수업의 페이지 구조 — 구현·배포 완료

2026-09-13 사용자는 위 내용을 바탕으로 빌드를 시작하며 전체 페이지 구조부터 바꾸는 순서를 제안했다. 당시 Codex가 구조·PRD를 정리하고 Gemini가 초기 코딩을 진행했다. 일회성 지시서는 사용자 승인으로 정리했으며, 당시 구현 범위는 이 절과 [1차 구조 검토서](docs/PLATFORM_STRUCTURE_REVIEW.md)에 보존한다. 현재 화면·로그인·기록 상태는 후속 9.7~9.13을 따른다.

**목표 구조**

* 학생의 주요 메뉴는 `수업 / 나의 기록 / 수행평가`이며 `교사용` 진입점은 보조 영역에 둔다. 브랜드는 수업 홈으로 이동한다.
* 수업 홈에는 여러 단원을 담을 목록을 두고, 현재 이용 가능한 문제 추상화·알고리즘 설계·순서도 연구소를 표시한다. `알고리즘`은 현재 콘텐츠의 화면상 주제 묶음이며 공식 교육과정 분류나 새 DB 계층을 확정하는 표현이 아니다.
* 단원 선택 → 단원 개요 → 개념·퀴즈·실습으로 연결한다. 기존 코스 ID와 실제 학습 화면을 유지한다. 차시별 활동 배정은 후속 기록 설계에서 연결하며, 아직 정하지 않은 1·2차시 구성을 임의로 만들지 않는다.
* 나의 기록은 향후 차시별 퀴즈·실습 제출물과 수행평가 결과를 보는 곳이다. 이번에는 경로와 준비 중 안내만 구현한다. 저장 기능 부재를 미제출·0점·기록 0건으로 표현하지 않는다.
* 수행평가는 기존 입장·시간 제한 평가로 연결한다. 교사용 경로는 현재 Google 인증·교사 권한 확인을 거쳐 기존 학급별 수행평가 관리로 연결한다.
* 교사 공간의 후속 목표는 `수업 관리 / 학습 기록 / 수행평가 관리`이다. 1차에서는 기존 수행평가 관리가 실제로 동작하게 연결하고, 미구현 관리 기능의 가짜 버튼·통계를 추가하지 않는다.

**1차 구현 범위와 보호할 동작**

수업 홈·단원 개요·공통 탐색·현재 위치 안내·기록 준비 중 화면을 구현한다. 새 페이지 이동에서도 기존 퀴즈·실습 상태, 순서도 다섯 단계, 평가 이동 잠금·새로고침 복구, 교사 화면 이탈 시 구독 해제를 유지한다. 실습·평가 편집기를 복제하거나 채점·인증·서버 저장 구조를 바꾸지 않는다.

첫 대표 화면은 수업 홈이다. 상단 탐색 아래 제목과 짧은 안내를 두고, 사용 가능한 단원을 제목·설명·진입 버튼으로 표시한다. 넓은 소개 배너·더미 성취 그래프를 추가하지 않는다. 단원 개요는 목표와 개념·퀴즈·실습 선택을 보여주며, 실습에서는 작업 영역 폭을 확보한다. 좁은 창에서는 메뉴를 접거나 줄바꿈하고 현재 위치와 주요 버튼을 사용할 수 있게 한다.

참고 원리: Google Classroom의 공식 안내에서 확인한 전체 과제 개요 → 학생별 목록 → 개별 제출물 확인 흐름을 후속 교사 기록 화면의 참고로 삼는다. 이는 본 서비스에 맞춘 설계 제안이며, Google Classroom 연동이나 외형 복제를 의미하지 않는다. [공식 도움말](https://support.google.com/edu/classroom/answer/9157286?hl=ko), 2026-09-13 확인. 이번에는 문서상 흐름을 참고했으며 실제 서비스 화면의 시각 검증으로 기록하지 않는다.

**후속 구현 순서**

1. 현재 1차: 페이지 구조와 기존 기능 연결을 로컬에서 검증한다.
2. 2차: 대표 단원의 차시·활동을 정하고 학생 식별·접근 권한·보관 범위를 확정한 뒤 퀴즈·실습 저장 → 교사 조회 → 학생 기록을 연결한다.
3. 3차: 관련 활동 묶음·제출 인정·예외 처리·환산 기준을 확정한 뒤 과정 5점과 수행평가 25·25·35점을 연결한다.

구현 상태: Gemini가 수업 홈·단원 개요·기록 준비 중 화면과 학습 진입을 구현했다. 2026-09-13 사용자가 점검·병합·배포를 요청하여 Codex가 교사 화면과 새 화면의 중첩, 좁은 창의 개요 복귀 버튼, 현재 메뉴 표시와 교사 인증 실패 시 탐색 상태를 보완했다. 전체 메뉴의 기존 접근 경로도 복원했다. 평가 배점·시간·도움 범위와 서버 자료 구조는 변경하지 않았다.

제품 `f656439`를 해커톤 Production 브랜치 `feature/hackathon`의 `74b1f11`에 병합·푸시했다. Vercel Production Ready, 새 도메인의 제품 파일 4개 일치와 1440·1024·768·390px의 주요 이동을 확인했다. 검증·배포 근거는 [1차 구조 검토서](docs/PLATFORM_STRUCTURE_REVIEW.md)를 따른다. 로컬 모의 검사를 새 Firebase의 실제 로그인·저장·AI 검증으로 해석하지 않는다. 홈의 정적 단원 목록과 `UNIT_META`는 아직 별도 정의이므로, 후속 단원 확장 시 단일 원본으로 정리해야 한다.

### 9.5 학생용 이름과 홈 안내 — 초기 변경 이력 (후속 배포는 9.7~9.13)

2026-09-13 사용자는 `교수평기 올인원`이 학생에게 낯설다며, 정보 놀이터처럼 수업을 즐기기 위한 공간이라는 인상을 원했다. 학생용 이름은 `정보 놀이터`로 정하고 브라우저 제목·홈 로고·홈 제목·푸터에 반영한다.

* 홈 소개: `생각을 펼치는 우리들의 정보 시간`
* 홈 안내: `퀴즈로 발견하고, 직접 만들며 배우는 정보 시간. 오늘 배울 내용을 골라 보세요.`
* 학생에게 설명이 필요한 교육 행정 용어보다 생각하기·만들기·발견하기처럼 활동을 떠올릴 수 있는 표현을 우선한다. 중학생에게 지나치게 유아적인 말투나 게임 보상 체계를 추가하지 않는다.
* `수업 / 나의 기록 / 수행평가 / 교사용`은 경로를 명확히 안내하는 이름으로 유지한다. 평가 중 도움·이동·제출 규칙은 그대로이다.

이번 변경은 `codex/student-learning-records` 브랜치의 로컬 수정이다. 이전 배포 승인을 이번 변경에 확대하지 않는다. 2026-09-13, `037ed92` 이후 로컬 수정본에서 문법·참조 검사 43개와 Edge 1440·768·390px의 제목·홈 표시·개요 진입·브랜드 홈 복귀를 확인했다. 페이지 가로 넘침과 JS 오류는 없었다. 화면은 `scratch/branding-1440.png`, `scratch/branding-390.png`에 남겼다. 커밋·푸시·배포는 하지 않았다.

### 9.6 대표 단원의 학습 기록 — 초기 설계·검증 이력

당시 Codex와 Gemini는 제출 데이터 구성 → 학생 인증 → 제출·조회 → 교사 조회로 작업을 나눴다. 이 절은 초기 단계의 결정과 검사 근거이며, 현재 기능은 9.7~9.9 및 [로그인·평가 기록](docs/HACKATHON_LOGIN_RELEASE.md), [전체 활동 기록](docs/ALL_UNIT_RECORDS_RELEASE.md)을 따른다. 완료된 단계별 지시서·중간 보고서는 2026-09-13 사용자 승인으로 정리했다.

**유지하는 설계 원칙**: 문제 추상화의 퀴즈 4문항, 학생의 현재·목표 상태, 조건과 작성 중 조건, 남긴 정보·버린 정보를 제출 시점 원본으로 기록한다. 오답·부분 작성도 보존하고 캡처 후 입력 변경이 이전 스냅샷을 바꾸지 않아야 한다. 학생 입력과 AI 생성 계획을 구분하며 클릭·키 입력·AI 대화 전체를 수집하지 않는다. 공식 과정점수는 별도 합의 없이 계산하지 않는다.

학생용 Google 로그인 사용이 어려워 학급·번호와 개인 비밀번호를 실제 Firebase 인증에 연결하는 방향을 선택했다. 학급·번호만으로 소유권을 인정하지 않고 서버 명부를 확인한다. 초안의 평면 명부·별도 인증 앱·27명 제한은 현재 구현 기준이 아니며 후속 확정 구조를 따른다.

**스냅샷 검증 이력**: js/core/learning-snapshot.js의 buildAbstractionSnapshot과 captureAbstraction을 구현했다. 2026-09-13, 기준 037ed92 이후 로컬 수정본에서 const/let 전역을 window 속성으로 읽어 값이 누락되는 오류와 평가 1·2부/복구 대기 잠금 누락을 재현·수정했다. 보기 정의가 없으면 선택 번호를 유효 답안으로 인정하지 않으며 정상적인 잠금 거부는 console.error를 남기지 않도록 보완했다.

당시 Node 관련 검사 5개, 문법·참조 검사 46개, 로컬 Edge 1280×900에서 4문항·부분 실습·등록 전 조건·정보 분리·복사 독립성 및 평가 진행/종료/복구 대기/공유 작업공간 잠금을 확인했다. JS·콘솔 오류 0건. 검사는 tests/learning-snapshot.test.cjs, 당시 로컬 실행 자료는 scratch/snapshot-check.cjs와 scratch/snapshot-review.png다. scratch 자료는 Git 제외이며 다른 PC에 없을 수 있다. 이 검증은 Firebase를 차단한 로컬 검사이고 운영 인증·저장 검증이 아니다. 이후 운영 검증은 후속 배포 기록으로 구분한다.

### 9.7 제출용 로그인·기록·평가 연결 — 2026-09-13

사용자가 Codex 직접 구현·커밋·병합·배포로 역할을 전환하고, 2학년 1~11반 각 28명(308개)의 개별 비밀번호 계정, Google 교사와 비상 교사 계정, 로그인 공급자·허용 도메인·DB 규칙 반영을 승인했다. 공통 초기 비밀번호 대신 학생별 무작위 비밀번호를 선택했다.

학생은 학급·번호·개인 비밀번호로 입장한다. 동일 Firebase UID로 일반 수업 제출과 수행평가를 연결하기 위해 기본 SESSION Auth를 공유한다. 교사 Google 로그인과 비밀번호 방식의 비상 로그인을 제공하며 서버 교사 역할을 확인한다. 추가 계정 발급만 별도 메모리 Auth에서 수행한다. 초기 2-2A 문서는 과거 계획으로 남긴다.

문제 추상화의 퀴즈·실습 원본 제출, 서버 성공 확인, 같은 요청 재시도, 자신의 최근 30개 제출 조회, 교사의 학생별 원본 조회를 구현했다. 정답 수는 현재 문항 버전의 기준으로 계산하며 공식 과정점수는 계산하지 않는다. 사용자 피드백에 따라 명부는 `learning_classes/{classId}/students/{두 자리 번호}`, 제출물은 그 학생 아래 `submissions/{uuid}`로 배치한다. UID는 인증 연결 필드로 유지한다. 계정 메모도 학급 폴더 → 학생별 파일로 나누며 308명을 한 파일에서 관리하지 않는다. 기존 평가 회차와 학습 기록은 분리한다.

기존 수행평가의 객관식·단답형·순서도 작성, 시간 제한, 교사 검토·확정·CSV를 유지하고 로그인한 학생의 학급·번호로만 입장시킨다. 관제실·회차 준비·API·서버 규칙의 학생 상한을 28번으로 확대했다. 학기 수행평가 3회 총점과 단일 순서도 평가 내부 배점은 별개이며 임의 변경하지 않는다.

실제 해커톤 Firebase에 308개 학생 계정, Google 및 비상 교사 권한을 등록하고 규칙을 컴파일·배포했다. 로컬 Edge에서 실제 로그인·수업 제출/조회·권한 거부·28번 평가 시작/제출·새 브라우저 기록 복귀·사용 종료를 확인했다. Google 팝업 자체는 미확인이다. 상세 근거, 제외 범위, 비공개 계정 배부표 위치는 [로그인·평가 배포 기록](docs/HACKATHON_LOGIN_RELEASE.md)을 따른다.


### 9.8 역할별 클래스룸과 학생 비밀번호 간소화 — 2026-09-13

사용자가 로그인 통합, 학생·교사별 클래스룸, 열린 평가 우선 표시, 제출 여부/정답률 분리, 대상 반이 명시된 평가 버튼의 구현·병합·배포를 승인했다. 추가 질문에서 학생에게 수행평가는 제출 여부만 표시하는 안을 선택했다.

상단 메뉴는 수업 / 내 클래스룸(교사는 우리 반 클래스룸)으로 정리한다. 우측 상단 로그인 창에서 학생 또는 교사를 선택한다. 로그인한 계정은 학급·번호 또는 교사 표시와 로그아웃 메뉴를 갖는다. 일반 수업은 비로그인 상태에서도 둘러볼 수 있다. 학생은 자기 학급의 현재 평가와 최근 수업 제출 3건을 확인하며 기록 화면에서 최근 30건을 조회한다. 열린 평가가 있으면 평가 입장을 최상단에, 제출 완료·대기 중이면 수업을 먼저 배치한다. 평가 상태의 서버 확인 실패는 대기나 미제출로 단정하지 않는다.

교사는 서버 권한을 확인한 뒤 11개 반 목록 → 반별 학생 기록 → 실시간 수행평가로 이동한다. 평가 준비·시작·종료 버튼과 새 회차 확인창에 대상 반을 표시한다. 상태 저장 중에는 반 변경·탭 이동·로그아웃을 막는다. 기존 평가 도움·배점·시간·채점·원본 보관·공유 PC SESSION 인증과 학급별 데이터 구조는 유지한다.

수업 기록은 제출 완료와 퀴즈 응답 수·정답률을 분리한다. 정답률은 참고 정보이며 과정점수로 자동 환산하지 않는다. 학생 평가 결과 화면에서 참고 점수와 세부 점수 표시를 제거했다. 이번 작업은 점수 공개 화면 정책이며, 기존 답안 문서의 점수 필드 및 클라이언트 채점 코드를 비공개 서버로 이전하는 기능은 아니다. 교사 공개 버튼과 학기별 평가 이력은 후속 범위이다.

학생 308명의 비밀번호를 모두 서로 다른 소문자·숫자 6자리로 변경하고 반별 명부와 학생별 메모를 갱신했다. 혼동 문자 0/o/1/l을 제외하고 두 문자 종류를 모두 포함한다. UID·명부·제출물·교사 비밀번호를 보존한다. 관리자 실행 도구는 반별 재시도 기록을 비공개 scratch 폴더에 남기며 Git과 배포에 포함하지 않는다. 기존 긴 학생 비밀번호는 더 이상 유효하지 않다. 길이가 짧아진 만큼 추측 저항성이 낮아지며 개인별 배부와 Firebase 로그인 제한을 유지한다.

검증: 기반 832a673 이후 codex/role-classrooms 수정본, Windows Edge 로컬 화면 + 실제 donghyun-hackthon Firebase. 실제 로그인 오류/성공, 역할별 11개 반·28명 조회, 학습 제출 원본, 학생 간 접근 거부, 평가 준비/시작/제출, 서버 제출 완료, 새 브라우저 기록 복귀, 로그아웃을 확인했다. 학생·교사 포털은 1440→360→1440px로 줄였다 늘리며 가로 넘침을 검사하고 대표 화면을 확인했다. Node 31개와 기존 평가 브라우저 10개 통과, JS·콘솔 오류 없음. 실서버 검증 자료 3개는 검증 후 정리했다. Google 팝업 실제 인증, 308명 동시 접속, 교사 비밀번호 재설정 화면은 미확인/미구현이다.

근거: scratch/release-qa/portal-live.json, scratch/portal-regression/assessment-ui.json 및 스크린샷. 배포 완료 여부와 최종 커밋은 최종 응답에서 별도로 보고한다. 관리자 비밀번호 변경 API는 [공식 문서](https://docs.cloud.google.com/identity-platform/docs/reference/rest/v1/projects.accounts/update)를 2026-09-13 확인했다.

### 9.9 전체 단원 기록과 기록 화면 개선 — 2026-09-13

사용자는 계속 남는 로그인 완료 문구를 없애고, 추상화에 국한된 기록을 모든 단원으로 확장하며 기존 띵커보드용 결과 버튼과 기록 저장을 연결해 달라고 요청했다. 각 단원의 퀴즈·실습에서 명시적으로 ‘내 기록에 저장’을 누르는 방식을 구현한다. 로그인 전 저장 요청은 당시 내용을 보존하고 인증 후 같은 화면에서 이어가며, 로그인 완료 문구는 제거하고 저장 성공 안내만 잠깐 표시한다.

3개 단원 퀴즈와 실습을 구분하고 추상화 원본, 학생 명령/로봇 해석, 순서도 기호·연결·자연어 카드 및 기초 활동 선택 상태를 보관한다. 나의 기록은 단원 필터와 활동별 요약·펼치기로 표시하고 교사도 같은 원본을 확인한다. 기존 추상화 기록의 호환성과 학급/학생/제출물 계층은 유지한다. 추가 작성 부담·클릭별 추적·자동 과정점수는 도입하지 않는다. 상세 데이터 범위·접근 규칙·실서버 검증 상태는 [전체 단원 기록 배포 기록](docs/ALL_UNIT_RECORDS_RELEASE.md)을 따른다.

낮은 우선순위로 요청한 ‘클릭·페이지 이동이 부드럽게 이어지는 UX’는 [UX 후속 작업](docs/UX_BACKLOG.md)에 계획으로 기록했다. 이번에 전체 화면 전환 애니메이션을 구현하거나 완료로 표기하지 않는다.

### 9.10 공간 탐색형 첫 화면과 전체 교육과정 — 구현, 2026-09-13

사용자는 차분한 UI를 유지하면서 첫 진입이 인상적이기를 원하며, 캐릭터를 방향키로 움직여 수업 공간으로 들어가는 방식을 제안했다. 사용자가 계획 기반 구현·독립 검토·병합·배포를 승인하여, 다섯 교육과정 영역을 좌우 대칭의 작은 2D 건물 지도에 배치하고 방향키 탐색과 클릭·키보드 직접 선택을 함께 제공한다. 현재 세 알고리즘 활동은 기존 경로에 연결하고, 나머지 주제는 직접 작성한 소개·핵심 질문과 실습 준비 상태를 제공한다. 신규 퀴즈·실습·다중 사용자 기능은 이번 범위에 포함하지 않는다.

교과서 PDF는 로컬 참고 전용이며 외부 업로드·배포를 금지한다. schoolbooks/의 Git·Vercel 제외를 명시하고 확인한다. 사용자 요청에 따라 브라우저 캡처는 변경한 시각·조작 부분에서 최소화하고 코드로 확인 가능한 사항은 논리 검토를 우선한다. AGENTS.md는 수정하지 않는다.

구체적인 화면·교육과정 구성·구현 순서·발표 동선은 [정보 놀이터 지도 계획](docs/HACKATHON_PLAYGROUND_PLAN.md)을 따른다. 5개 영역의 총 25개 주제를 자체 설명·질문으로 제공하며, 기존 3개 활동 외의 실습은 준비 중으로 표시한다. 건물은 자체 SVG이며 게임 엔진·외부 이미지·실시간 아바타 공유는 도입하지 않았다. 화면 이탈·포커스 해제·탭 숨김에서 이동을 멈추고, 기존 평가·교사 작업 잠금을 지킨다. OS 움직임 줄이기가 켜져 있으면 기기 설정 상태를 명시하고 우선 적용한다. 상세 검증과 배포 근거는 [지도 홈 배포 기록](docs/PLAYGROUND_RELEASE.md), 독립 평가는 [검토 기록](docs/PLAYGROUND_REVIEW.md)을 따른다.

### 9.11 넓은 픽셀아트 대문 — 렌더링 단계, 2026-09-13

사용자는 현재 대문이 작고 벡터 그림이 기대한 느낌과 다르다며, 첨부한 ZEP 계열 이미지를 참고한 섬세한 픽셀아트·큰 맵·상하좌우가 구분되는 캐릭터를 요청했다. 먼저 캐릭터·맵·건물을 렌더링한 뒤 단계적으로 구현한다. 5건물 맵과 캐릭터 4방향/걷기 시안을 생성해 로컬 scratch에 보관했다. 아직 운영 앱에 반영하지 않았다.

PC에서는 대문 너비 약 90%와 넓은 맵을 목표로 하고, 기존 화면 높이 제한에 의한 축소를 대체한다. 비활성 ‘기기 설정: 움직임 줄이기’ 버튼은 제거하되 기기의 접근성 설정은 내부에서 자동 존중할 계획이다. 시안과 프레임 정렬·문 좌표·넓은 화면 적용 순서는 [픽셀아트 대문 방향](docs/PIXEL_PLAYGROUND_DIRECTION.md)을 따른다. 9.10은 현재 운영 상태, 9.11은 다음 변경 방향이다.


### 9.12 픽셀 대문·정문 출발·발표용 체험 — 구현, 2026-09-13

사용자가 픽셀 시안을 토대로 정문 출발, 모달 대신 수업 페이지, 빌드·병합·배포를 승인하고 발표용 학생/교사 진입을 추가 요청했다. 넓은 픽셀 맵과 4방향 캐릭터를 적용하고 시작 위치를 하단 정문으로 옮겼다. 새 맵 위치는 sessionStorage v2로 분리한다. 건물 입장은 독립 수업 페이지에 연결하고 기존 세 활동/25주제를 유지한다. 불필요한 모션 설정 버튼은 제거하고 기기 설정을 자동 존중한다. 걷기는 방향별 정지 그림과 짧은 움직임 효과이며 발걸음 프레임 애니메이션은 후속이다.

로그인 창의 학생 체험/교사 체험은 샘플 데이터만 사용하는 별도 화면이다. 학생의 3단원 기록·샘플 평가 제출, 교사의 11반/28명 샘플 기록과 평가 시작을 시연한다. 실제 Firebase 인증이나 권한을 부여하지 않고 서버 기록을 쓰지 않는다. 실제 인증/학생 데이터는 보존하며 화면에서 체험임을 명시한다.

검증: 지도 독립 검사11개 통과(정문부터5문, 수업페이지, 복귀포커스, 잠금, 위치복구). 로컬 내장브라우저에서 넓은맵/정문로봇 시각 확인, 모달없는 알고리즘페이지와3활동, 학생샘플제출완료, 교사11반/28명과시작을 확인했다. 해당흐름콘솔오류0. 교과서/비밀번호는 배포에 포함하지 않는다. 운영커밋과READY상태는 최종응답에서 별도로 확인한다.

### 9.13 실제 게스트 로그인 — 운영 활성화 · 2026-09-13

샘플 체험 대신 미리 준비한 게스트 학생/교사로 실제 기능을 사용하도록 변경한다. 전용 발표반 `2-12`의 고정 계정에 서버 custom token으로 로그인하고 기존 수업 기록·평가·교사 관제실에 연결한다. 게스트 교사는 발표반만 조회·운영하며 기존 1~11반 자료에는 접근할 수 없도록 Firestore와 평가 API에서 제한한다. 공용 게스트 데이터임을 알리고 실제 개인정보 입력을 피하도록 안내한다. 기존 학생 비밀번호·교사 계정·평가 정책은 유지한다.

최초에는 사용자 지시에 따라 작업 브랜치에 구현·검증·커밋까지만 진행했다. 이후 사용자의 **go!** 승인에 따라 접근 규칙을 먼저 배포하고 전용 계정·역할·서명 키와 Vercel 서버 환경을 설정한 뒤 운영 브랜치에 병합·배포했다. 이전 9.12의 샘플 전용 화면을 실제 게스트 로그인으로 대체했다.

Node 검사 56개, 문법·자산 검사, Firebase 규칙 컴파일과 실제 인증·접근·기록 저장 검사 33개를 통과했다. 운영 브라우저에서 학생/교사 SDK 로그인, 발표반 조회, 평가 대기·시작·작성·최종 제출과 교사 수신을 확인하고 검증 회차를 보관한 뒤 새 회차를 준비했다. 운영 READY와 로그인 API HTTP 200을 확인했다. 구현 범위·설정 순서·일시적 연결 경고 등 검증 한계는 [게스트 로그인 배포 기록](docs/GUEST_LOGIN_BRANCH.md)을 따른다.

### 9.14 첫 화면 높이 맞춤·이동 유도·AI 튜터 탭 — 운영 배포·검증 완료, 2026-09-13

사용자는 현재 픽셀 지도 창이 첫 화면보다 커서 하단이 보이지 않는 문제를 지적하고, 모바일·디벗·PC 어디서든 첫 진입에 캐릭터를 움직이는 화면임을 바로 이해할 수 있도록 요청했다. 전역 헤더와 중복되는 큰 대문 소개 문구는 제거하고, 현재 16:9 맵 전체와 출발 로봇·필수 조작부를 동적 화면 높이에 맞춘다. 첫 이동 전에는 행동 중심 안내를 지도 안에 표시하며 모바일·터치 환경에는 기존 이동 엔진을 공유하는 방향 패드를 제공한다. 건물 클릭과 수업 바로가기는 대체 경로로 유지한다.

AI 튜터는 호버 가능한 환경에서 우측에 아이콘 손잡이만 남겨 두고 닫힌 상태의 한글 라벨은 완전히 감춘다. 호버·키보드 초점·열림 상태에서만 라벨과 함께 옆으로 펼친다. 터치 환경에는 아이콘 버튼을 온전히 표시해 한 번 탭으로 연다. 움직임 줄이기, 접근성 이름, 수행평가 중 숨김을 유지한다. 구현 범위·보호할 동작·뷰포트별 완료 조건은 [구현·검토 기록](docs/HOME_VIEWPORT_TUTOR_PLAN.md)을 따른다.

Gemini가 `a582573`에서 초안을 구현했으나 지도·건물·캐릭터·수업 페이지의 기존 CSS 대부분이 삭제되고 모바일 방향 버튼이 실제 이동 포커스 조건을 충족하지 못했으며, 신규 테스트와 문서 상태 갱신이 빠져 있었다. Codex가 `codex/review-home-viewport-tutor`에서 기존 시각 구조를 복원하고 화면 높이 계산, 키보드·포인터 공통 이동, 첫 이동 안내, 튜터 접근성 상태를 보완했다. 문법·자산 검사와 관련 16개, 전체 61개 Node 검사를 통과했다. 로컬 Chrome에서 `1920×900`, `1366×650`, `1024×768`, `768×1024`, `390×844`, `360×740`의 지도 창 맞춤·가로 넘침을 수치로 확인하고, 넓은 화면 지도·수업 페이지, 모바일 방향 버튼 이동, 튜터 열기·닫기를 시각·조작 확인했다. 콘솔 오류는 없고 기존 Tailwind CDN 경고만 남았다. 운영 병합·배포·Firebase는 변경하지 않았다.

후속 화면 확인에서 접힌 튜터의 한글 일부가 노출된 문제를 수정하고, 로봇 방향 이미지에 Chrome 호환 마스크 규칙을 추가했다. 관련 18개와 전체 63개 Node 검사 및 문법·자산 검사를 통과했다. `file://` 자동 재확인은 브라우저 보안 정책으로 실행하지 못했으며 운영 환경과 실제 모바일 기기는 여전히 미확인이다. 이후 작업은 별도 요청이 없는 한 Codex가 계획부터 구현·검토·검증까지 담당하며 Gemini 릴레이는 기본 절차로 사용하지 않는다.

사용자 확인에서 로컬 파일 화면의 로봇은 여전히 보이지 않았다. 로컬 파일의 외부 SVG 마스크 제한 가능성은 있으나 원인으로 확정하지 않으며, 추가 이미지 변경은 보류하고 Vercel 배포 뒤 표시 여부를 먼저 확인한다. 튜터 손잡이는 버튼의 감속 이동과 라벨의 투명도·수평 이동이 자연스럽게 이어지도록 보완하고 움직임 줄이기 설정에서는 즉시 전환한다.

사용자 승인 후 `codex/review-home-viewport-tutor`를 원격에 푸시하고 `feature/hackathon`에 병합했다. 운영 병합 커밋은 `a5e8fd6`이다. Vercel의 HTTP 200과 최신 스타일 제공을 확인했으며, 운영 Chrome에서 로봇이 정문에 표시되고 방향키 입력에 이동하는 것을 확인했다. 첫 이동 안내는 이동 뒤 사라졌고 AI 튜터는 닫힘 시 아이콘만 표시되며 한 번 클릭으로 정상 열렸다. 해당 흐름의 콘솔 오류는 0건이었다. 따라서 로봇 미표시는 로컬 `file://` 환경에 한정된 것으로 확인하며 운영 코드의 로봇 이미지는 추가 변경하지 않는다.
